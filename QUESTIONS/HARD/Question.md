🔴 Hard Level (1-50)
Build a multi-user banking application with persistent accounts and transactions.
Build a password manager with encrypted local storage.
Build a secure login system with password hashing, sessions, and account locking.
Build a command-line expense tracker with persistent storage and monthly reports.
Build a Java application that reads and validates complex configuration files.
Build a CSV parser that handles quoted fields, escaped quotes, and malformed rows.
Build a JSON parser from scratch without using a JSON library.
Build a JSON serializer and deserializer using Java reflection.
Build a mini template engine that replaces variables and evaluates simple expressions.
Build a mathematical expression evaluator that supports variables, functions, and parentheses.
Build a plugin system where external JAR files can be discovered and loaded at runtime.
Build a dependency-injection container using Java reflection.
Build an event-driven application using the Observer and Event Bus patterns.
Build a configurable logging framework with multiple output destinations.
Build a command framework supporting undo and redo operations.
Build a rule engine where users can define and execute business rules.
Build a workflow engine where tasks can depend on the completion of other tasks.
Build a permission system supporting users, roles, and fine-grained permissions.
Build a modular notification system supporting multiple notification providers.
Build a Java application with clean architecture and interchangeable persistence layers.
Build a multithreaded file downloader.
Build a thread-safe bank transaction system.
Build a producer-consumer application using BlockingQueue.
Build a thread pool from scratch using Java concurrency primitives.
Build a scheduled task executor with task cancellation.
Build a concurrent web crawler with rate limiting.
Build a thread-safe in-memory cache with expiration.
Build a job-processing system where multiple workers process tasks concurrently.
Build a system that detects and safely handles concurrent transaction conflicts.
Build a multithreaded log-processing application that processes large files concurrently.
Build a TCP chat application with multiple clients.
Build a client-server file transfer application.
Build an HTTP server from scratch using Java sockets.
Build a REST API using Spring Boot with authentication and persistent data.
Build a WebSocket-based real-time notification system.
Build a network monitoring application that checks whether configured services are reachable.
Build a client-server multiplayer game using Java networking.
Build an API gateway that forwards requests to different backend services.
Build a rate-limiting system for incoming API requests.
Build a service-health monitoring dashboard.
Build a Java application with MySQL transactions and rollback support.
Build a database migration system that tracks and applies schema versions.
Build a library-management backend with users, books, borrowing, and transaction history.
Build an inventory-management backend with persistent stock updates.
Build a REST API with authentication, authorization, validation, pagination, and database persistence.
Build a backend that imports thousands of records from CSV into MySQL while reporting invalid records.
Build an audit-log system that records important changes made by users.
Build a backend with optimistic locking to prevent conflicting updates.
Build a complete authentication service with registration, login, logout, password reset, and session management.
Build a production-style Java backend with REST APIs, database persistence, validation, logging, exception handling, authentication, testing, and configuration management.